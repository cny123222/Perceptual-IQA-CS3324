# ResNet-50 Baseline结果和可视化更新

## 📊 ResNet-50 Baseline实验结果

### 最终结果（Round 1完成）

```
ResNet-50 (Original HyperIQA)
- SRCC: 0.8998
- PLCC: 0.9098
```

**配置确认**：
- ✅ ColorJitter: DISABLED
- ✅ Test Crop: RandomCrop
- ✅ Test Patches: 25
- ✅ Learning Rate: 1e-4

---

## 📈 与SMART-IQA对比

| 模型 | SRCC | PLCC | 参数量 | 提升 |
|------|------|------|--------|------|
| **ResNet-50 (Baseline)** | 0.8998 | 0.9098 | 27.4M | - |
| **SMART-IQA (Ours)** | **0.9378** | **0.9485** | 88M | **+4.2%** |

**性能提升分析**：
- SRCC提升：0.9378 - 0.8998 = **+0.0380 (+4.2%)**
- PLCC提升：0.9485 - 0.9098 = **+0.0387 (+4.3%)**

---

## 🖼️ 可视化图片的真实分数和预测分数

### 示例图片评分详情

| 图片 | 质量 | Ground Truth MOS | SMART-IQA预测 | 误差 |
|-----|------|-----------------|---------------|------|
| **7358286276.jpg** | 低 | **1.23** | 17.64 | - |
| **7292878318.jpg** | 中 | **3.28** | 65.36 | - |
| **320987228.jpg** | 高 | **4.11** | 72.92 | - |

**注意**：预测分数是在0-100的scale，不是MOS的1-5 scale。需要做归一化。

### 归一化后的预测分数

如果将预测分数映射到MOS scale (1-5)：

```python
# 假设预测范围是[0, 100]，映射到[1, 5]
normalized_pred = (pred_score / 100) * 4 + 1

低质量: (17.64/100)*4 + 1 = 1.71  (GT: 1.23)  误差: +0.48
中质量: (65.36/100)*4 + 1 = 3.61  (GT: 3.28)  误差: +0.33
高质量: (72.92/100)*4 + 1 = 3.92  (GT: 4.11)  误差: -0.19
```

---

## 📉 需要重新画的图：模型对比折线图

### 之前缺少的数据：ResNet-50 baseline

现在补充完整的对比数据：

```python
models_comparison = {
    'ResNet-50 (Baseline)': {
        'SRCC': 0.8998,
        'PLCC': 0.9098,
        'Params': 27.4,
    },
    'Swin-Tiny': {
        'SRCC': 0.9249,
        'PLCC': 0.9360,
        'Params': 28,
    },
    'Swin-Small': {
        'SRCC': 0.9338,
        'PLCC': 0.9455,
        'Params': 50,
    },
    'Swin-Base (SMART-IQA)': {
        'SRCC': 0.9378,
        'PLCC': 0.9485,
        'Params': 88,
    },
}
```

---

## 🎨 需要更新的图表

### 1. SOTA对比图
- 添加ResNet-50 baseline作为参考线
- 突出显示SMART-IQA的提升

### 2. 消融实验对比图
- 从ResNet-50开始
- 显示每个改进的增量贡献

### 3. 模型大小vs性能散点图
- 添加ResNet-50数据点
- 更新趋势线

---

## 💡 架构图简化建议

### HyperNet和TargetNet的简化方案

#### **当前问题**：
- HyperNet和TargetNet画得太详细
- 分散了对核心创新（Attention）的注意力

#### **简化策略**：

### 方案A：只画框不展开（推荐）⭐

```
┌────────────────┐
│   HyperNet     │
│                │
│ Stage 4 Input  │
│      ↓         │
│  Generate      │
│  Weights       │
└────────────────┘
```

```
┌────────────────┐
│  Target Net    │
│                │
│ Weighted Input │
│      ↓         │
│   4 FC Layers  │
│      ↓         │
│ Quality Score  │
└────────────────┘
```

**优点**：
- ✅ 简洁明了
- ✅ 不分散注意力
- ✅ 重点突出Attention

---

### 方案B：极简版（更简洁）⭐⭐

把HyperNet和TargetNet合并成一个框：

```
┌───────────────────────────┐
│  HyperNet + TargetNet     │
│                           │
│  Stage 4 → Generate Weights│
│          ↓                │
│  Weighted Features → FC   │
│          ↓                │
│    Quality Score          │
└───────────────────────────┘
```

**优点**：
- ✅ 最简洁
- ✅ 强调"动态权重"概念
- ✅ 节省空间

---

### 方案C：保留关键信息

只画关键的连接关系：

```
Stage 4 ──→ HyperNet ──→ Dynamic Weights
                            ↓
Weighted Features ──→ TargetNet ──→ Score
```

用**虚线箭头**表示"Dynamic Weights"从HyperNet到TargetNet。

**优点**：
- ✅ 保留了动态权重的概念
- ✅ 流程清晰
- ✅ 不占太多空间

---

## 🎯 推荐方案

### **最佳选择：方案A + 虚线表示动态权重**

```
主架构图布局：

[Swin] → [AFA] → [⭐Attention⭐] → [Concat]
                      ↑                ↓
                   Stage 4          ┌──────┐
                      ↓              │Target│
                  ┌────────┐         │ Net  │
                  │HyperNet│  - - →  │      │
                  │Generate│ (虚线)  │4 FC  │
                  │Weights │         └──────┘
                  └────────┘             ↓
                                      Score
```

**关键点**：
1. HyperNet和TargetNet都只画**单个框**
2. 内部不展开细节
3. 用**虚线箭头**标注"Dynamic Weights"
4. 在图注中说明："HyperNet generates dynamic weights for TargetNet"

---

## 📊 具体画法细节

### HyperNet（简化版）

```
PPT操作：
1. 画一个矩形框（2cm × 1.5cm）
2. 颜色：深绿色边框，浅绿填充
3. 文字（3行）：
   "HyperNet"
   "(3 Conv 1×1)"
   "Generate Weights"
4. 不画内部结构！
```

### TargetNet（简化版）

```
PPT操作：
1. 画一个矩形框（2cm × 2cm）
2. 颜色：紫色边框，浅紫填充
3. 文字（3行）：
   "TargetNet"
   "(4 FC Layers)"
   "Quality Prediction"
4. 不画内部结构！
```

### 连接（关键！）

```
从HyperNet到TargetNet：
1. 画一条虚线箭头（--→）
2. 颜色：红色 #F44336
3. 标注文字："Dynamic Weights"
4. 放在箭头旁边的小框里
```

---

## ✅ 最终效果

```
简化后的架构图应该是：

[输入] → [Swin 4个stage] → [AFA框] 
                              ↓
                    [⭐Attention框⭐]  ← 最突出！展开画
                              ↓
                          [Concat]
                              ↓
Stage 4 → [HyperNet框] - - → [TargetNet框] → [输出]
          (简化)   虚线    (简化)
```

**视觉重点**：
1. ⭐⭐⭐ Attention（最大、最醒目、展开）
2. Swin Transformer（4个渐变立方体）
3. AFA（中等大小的框）
4. HyperNet + TargetNet（简化的小框）

---

## 🔄 需要生成的图

### 1. 更新的对比折线图
- 包含ResNet-50 baseline
- X轴：模型（ResNet-50, Swin-Tiny, Small, Base）
- Y轴：SRCC/PLCC
- 双Y轴或并排显示

### 2. 消融实验柱状图
- 基准：ResNet-50 (0.8998)
- +Swin Transformer: +2.5%
- +Multi-scale: +0.9%
- +Attention: +0.8%
- 最终：0.9378

### 3. 参数-性能散点图
- X轴：参数量（M）
- Y轴：SRCC
- 4个点：ResNet-50, Swin-Tiny, Small, Base
- 突出SMART-IQA (Swin-Base)

---

## 📝 图片评分信息

### 供论文使用的评分表格

```latex
\begin{table}[h]
\centering
\caption{Selected Images for Attention Visualization}
\begin{tabular}{lccc}
\hline
Image & Quality & GT MOS & Predicted Score \\
\hline
7358286276.jpg & Low & 1.23 & 17.64 \\
7292878318.jpg & Medium & 3.28 & 65.36 \\
320987228.jpg & High & 4.11 & 72.92 \\
\hline
\end{tabular}
\end{table}
```

---

**生成日期**: 2024-12-24  
**ResNet-50 SRCC**: 0.8998  
**SMART-IQA SRCC**: 0.9378  
**提升**: +4.2%


# 架构图绘制指南 - 详细版

**目的**: 为论文绘制架构图提供详细的技术说明  
**日期**: 2025-12-23

---

## 📐 问题1: 架构详细说明 - 用于绘制架构图

### 🔍 整体对比：原始 vs 我们的改进

```
原始HyperIQA架构:
Input Image (224×224) 
    ↓
ResNet50 Backbone (CNN)
    ├─ Layer1 (256-D)  ──→ LDA1 ──┐
    ├─ Layer2 (512-D)  ──→ LDA2 ──┤
    ├─ Layer3 (1024-D) ──→ LDA3 ──┼──→ Concat → target_in_vec (112-D)
    └─ Layer4 (2048-D) ──→ LDA4 ──┘
         │
         └──→ hyper_in_feat (2048-D, 7×7) → HyperNet → 动态权重
                                                            ↓
                                                    TargetNet (target_in_vec + 动态权重)
                                                            ↓
                                                      Quality Score

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

我们的改进架构 (Swin-HyperIQA):
Input Image (224×224)
    ↓
Swin Transformer Backbone (Vision Transformer) [改进1: Backbone替换]
    ├─ Stage 0 (128-D, 56×56) ──┐
    ├─ Stage 1 (256-D, 28×28) ──┤
    ├─ Stage 2 (512-D, 14×14) ──┼──→ Multi-scale Fusion [改进2: 真正的多尺度]
    └─ Stage 3 (1024-D, 7×7)  ──┘        ↓
         │                          Adaptive Pooling (all to 7×7)
         │                                 ↓
         │                          Concatenate (1920-D, 7×7) [Base model]
         │                                 ↓
         │                          Channel Attention [改进3: 注意力融合]
         │                                 ↓
         │                          hyper_in_feat (1920-D, 7×7)
         │                                 ↓
         │                            Conv1x1 Blocks
         │                                 ↓
         │                          Compressed feat (112-D)
         │                                 ↓
         └──→ target_in_vec (112-D) ──→ HyperNet → 动态权重 [改进4: Dropout正则化]
                                                      ↓
                                              TargetNet (target_in_vec + 动态权重)
                                                      ↓
                                                Quality Score
```

---

## 🎨 详细模块说明 - 用于画图

### 模块1: Input & Patch Embedding

**原始HyperIQA**:
```
Input: [B, 3, 224, 224]
  ↓ Conv 7×7, stride=2
  ↓ BN + ReLU
  ↓ MaxPool 3×3, stride=2
Output: [B, 64, 56, 56]
```

**我们的改进**:
```
Input: [B, 3, 224, 224]
  ↓ Patch Partition (4×4 patches)
  ↓ Linear Embedding
Output: [B, 96, 56, 56] (Tiny/Small) 或 [B, 128, 56, 56] (Base)
```

**画图建议**: 
- 用**橙色**表示原始的Conv层
- 用**蓝色**表示我们的Patch Embedding
- 标注"4×4 patch, stride=4"

---

### 模块2: 特征提取 Backbone (核心改进!)

#### 2.1 原始 ResNet50

```
结构:
┌─────────────────────────────────────────┐
│  Input: [B, 64, 56, 56]                  │
│                                          │
│  Layer1 (64 → 256):                      │
│  ├─ Bottleneck × 3                       │
│  └─ Output: [B, 256, 56, 56]             │
│        ↓                                 │
│  Layer2 (256 → 512):                     │
│  ├─ Bottleneck × 4                       │
│  └─ Output: [B, 512, 28, 28]             │
│        ↓                                 │
│  Layer3 (512 → 1024):                    │
│  ├─ Bottleneck × 6                       │
│  └─ Output: [B, 1024, 14, 14]            │
│        ↓                                 │
│  Layer4 (1024 → 2048):                   │
│  ├─ Bottleneck × 3                       │
│  └─ Output: [B, 2048, 7, 7] ◀── 只有这个进HyperNet!
└─────────────────────────────────────────┘

关键问题:
❌ 只用了Layer4的2048-D特征
❌ 其他层特征被LDA压缩成112-D标量 (损失空间信息)
❌ CNN的局部感受野限制了全局理解
```

**画图建议**:
- 用**灰色方块**表示Bottleneck blocks
- Layer1-3用**虚线箭头**指向LDA (表示信息压缩)
- Layer4用**实线粗箭头**指向HyperNet (强调这是主要路径)
- 标注"仅Layer4 (2048-D) → HyperNet"

---

#### 2.2 我们的 Swin Transformer (⭐ 核心改进)

```
结构 (Base模型):
┌─────────────────────────────────────────────────────────────┐
│  Input: [B, 128, 56, 56]                                     │
│                                                              │
│  Stage 0: Swin Transformer Blocks × 2                        │
│  ├─ Window-MSA (7×7 windows)                                 │
│  ├─ Shifted Window-MSA                                       │
│  ├─ MLP (FFN with GELU)                                      │
│  └─ Output: [B, 128, 56, 56] ────────┐                       │
│        ↓ Patch Merging (2×2→1)       │                       │
│                                       │                       │
│  Stage 1: Swin Transformer Blocks × 2│                       │
│  └─ Output: [B, 256, 28, 28] ────────┼───┐                   │
│        ↓ Patch Merging                │   │                   │
│                                       │   │                   │
│  Stage 2: Swin Transformer Blocks × 18│   │                   │
│  └─ Output: [B, 512, 14, 14] ────────┼───┼───┐               │
│        ↓ Patch Merging                │   │   │               │
│                                       │   │   │               │
│  Stage 3: Swin Transformer Blocks × 2│   │   │               │
│  └─ Output: [B, 1024, 7, 7] ─────────┼───┼───┼───┐           │
│                                       │   │   │   │           │
│  ═══════════════════════════════════════════════════         │
│  多尺度融合 (Multi-scale Fusion):     │   │   │   │           │
│                                       ↓   ↓   ↓   ↓           │
│  ┌─────────────────────────────────────────────────┐         │
│  │ Adaptive Pooling to 7×7:                        │         │
│  │ ├─ feat0: [B, 128, 7, 7]                        │         │
│  │ ├─ feat1: [B, 256, 7, 7]                        │         │
│  │ ├─ feat2: [B, 512, 7, 7]                        │         │
│  │ └─ feat3: [B, 1024, 7, 7]                       │         │
│  └─────────────────────────────────────────────────┘         │
│                      ↓                                        │
│  ┌─────────────────────────────────────────────────┐         │
│  │ Channel Attention (可选):                       │         │
│  │ 1. GAP on feat3 → [B, 1024]                     │         │
│  │ 2. FC: 1024→256→4 (Softmax)                     │         │
│  │ 3. 生成4个注意力权重: [B, 4]                    │         │
│  │ 4. 对4个feat加权后拼接                          │         │
│  └─────────────────────────────────────────────────┘         │
│                      ↓                                        │
│  Concatenate: [B, 1920, 7, 7] ◀── 所有stage都进HyperNet! ✅  │
└─────────────────────────────────────────────────────────────┘

关键优势:
✅ 使用所有4个stage的完整空间特征 (1920-D)
✅ Shifted Window Attention 实现全局建模
✅ 层级式特征融合 (低层纹理 + 高层语义)
✅ 可选的Channel Attention动态加权
```

**画图建议**:
- 用**蓝绿色方块**表示Swin Transformer Blocks
- 内部画小图标: `W-MSA → SW-MSA → MLP`
- 用**4条彩色箭头**从4个stage指向融合模块:
  - 浅蓝 (Stage 0, 低层)
  - 蓝色 (Stage 1, 中层)
  - 深蓝 (Stage 2, 中高层)
  - 紫色 (Stage 3, 高层)
- **Attention模块**用**金色**高亮框
- 标注"All stages → 1920-D multi-scale features"

---

### 模块3: Multi-Scale Fusion (⭐ 改进2)

**原始HyperIQA的LDA模块**:
```
┌───────────────────────────────────────────┐
│ Local Distortion Aware (LDA):             │
│                                           │
│ Layer1 [B,256,56,56]                      │
│   ↓ Conv1×1 (256→16)                      │
│   ↓ AvgPool 7×7                           │
│   ↓ Flatten → FC                          │
│   → lda1 [B, 28]                          │
│                                           │
│ Layer2/3/4 类似...                        │
│                                           │
│ Concat → target_in_vec [B, 112] ✗        │
│                                           │
│ 问题:                                     │
│ ❌ 空间信息被池化和压缩丢失               │
│ ❌ 多尺度特征仅用于TargetNet              │
│ ❌ HyperNet只用单尺度 (Layer4)            │
└───────────────────────────────────────────┘
```

**我们的Multi-Scale Fusion**:
```
┌───────────────────────────────────────────────────────┐
│ Multi-Scale Feature Fusion:                          │
│                                                       │
│ Stage 0: [B, 128, 56, 56]                            │
│     ↓ AdaptiveAvgPool2d(7,7)                         │
│     → feat0: [B, 128, 7, 7] ──┐                      │
│                                │                      │
│ Stage 1: [B, 256, 28, 28]     │                      │
│     ↓ AdaptiveAvgPool2d(7,7)  │                      │
│     → feat1: [B, 256, 7, 7] ──┼──┐                   │
│                                │  │                   │
│ Stage 2: [B, 512, 14, 14]     │  │                   │
│     ↓ AdaptiveAvgPool2d(7,7)  │  │                   │
│     → feat2: [B, 512, 7, 7] ──┼──┼──┐                │
│                                │  │  │                │
│ Stage 3: [B, 1024, 7, 7]      │  │  │                │
│     → feat3: [B, 1024, 7, 7] ─┼──┼──┼──┐             │
│                                │  │  │  │             │
│                                ↓  ↓  ↓  ↓             │
│  ┌─────────────────────────────────────────┐         │
│  │ 选项1: 简单拼接 (--no_attention_fusion) │         │
│  │ Concat(dim=1) → [B, 1920, 7, 7]         │         │
│  └─────────────────────────────────────────┘         │
│                      OR                               │
│  ┌─────────────────────────────────────────┐         │
│  │ 选项2: 注意力加权 (--attention_fusion)  │         │
│  │                                          │         │
│  │ 1. Global feature from feat3:           │         │
│  │    GAP(feat3) → [B, 1024]               │         │
│  │                                          │         │
│  │ 2. Attention network:                   │         │
│  │    ┌──────────────────────┐             │         │
│  │    │ FC: 1024 → 256       │             │         │
│  │    │ ReLU                 │             │         │
│  │    │ Dropout(0.5)         │             │         │
│  │    │ FC: 256 → 4          │             │         │
│  │    │ Softmax              │             │         │
│  │    └──────────────────────┘             │         │
│  │         ↓                                │         │
│  │    weights: [B, 4] (sum to 1)           │         │
│  │                                          │         │
│  │ 3. Weighted fusion:                     │         │
│  │    feat_i' = feat_i × weight_i          │         │
│  │    Concat(feat0', ..., feat3')          │         │
│  │         ↓                                │         │
│  │    [B, 1920, 7, 7] ✅                    │         │
│  └─────────────────────────────────────────┘         │
│                                                       │
│ 优势:                                                 │
│ ✅ 保留所有stage的空间信息 (7×7)                      │
│ ✅ 多尺度特征直接用于HyperNet (1920-D)                │
│ ✅ Channel Attention动态调整各尺度重要性              │
│ ✅ 端到端可学习的融合策略                             │
└───────────────────────────────────────────────────────┘
```

**画图建议**:
- 用**4个不同大小的方块**表示4个stage (56×56, 28×28, 14×14, 7×7)
- 用**虚线箭头**表示AdaptivePooling到7×7
- **Attention模块**用单独的**金色矩形框**，内部画:
  - `GAP → FC(1024→256) → ReLU → Dropout → FC(256→4) → Softmax`
- 用**渐变色条**表示attention weights (0~1)
- 最后用**宽箭头**指向拼接后的1920-D特征

---

### 模块4: HyperNet (动态权重生成)

**原始 vs 改进对比**:

```
┌────────────────────────────────────────────────────────────┐
│ 原始HyperNet:                                              │
│                                                            │
│ Input: hyper_in_feat [B, 2048, 7, 7] (仅Layer4)            │
│   ↓                                                        │
│   Conv 2048→1024→512→112 (3个1×1 Conv + ReLU)             │
│   ↓                                                        │
│   Compressed: [B, 112, 7, 7]                               │
│   ↓                                                        │
│   生成TargetNet的动态权重:                                 │
│   ├─ fc1_weight: Conv3×3 → [B, 112×16, 7, 7]              │
│   ├─ fc1_bias:   FC → [B, 16]                             │
│   ├─ fc2_weight, fc2_bias (16→8)                          │
│   ├─ fc3_weight, fc3_bias (8→4)                           │
│   ├─ fc4_weight, fc4_bias (4→2)                           │
│   └─ fc5_weight, fc5_bias (2→1) → 质量分数                │
│                                                            │
│ 问题:                                                      │
│ ❌ 输入特征单一 (仅2048-D)                                 │
│ ❌ 无正则化 (容易过拟合)                                   │
└────────────────────────────────────────────────────────────┘

VS

┌────────────────────────────────────────────────────────────┐
│ 改进的HyperNet:                                            │
│                                                            │
│ Input: hyper_in_feat [B, 1920, 7, 7] (多尺度融合) ✅       │
│   ↓                                                        │
│   Conv 1920→512→256→112 (3个1×1 Conv + ReLU)              │
│   ↓                                                        │
│   Dropout(0.4) ◀── 新增正则化! ✅                          │
│   ↓                                                        │
│   Compressed: [B, 112, 7, 7]                               │
│   ↓                                                        │
│   生成TargetNet的动态权重 (结构相同):                      │
│   ├─ fc1_weight/bias                                      │
│   ├─ fc2_weight/bias                                      │
│   ├─ fc3_weight/bias                                      │
│   ├─ fc4_weight/bias                                      │
│   └─ fc5_weight/bias                                      │
│                                                            │
│ 改进:                                                      │
│ ✅ 输入特征更丰富 (1920-D 多尺度)                          │
│ ✅ Dropout防止过拟合 (0.4比例)                             │
│ ✅ 更强的特征表示能力                                      │
└────────────────────────────────────────────────────────────┘
```

**画图建议**:
- 用**紫色方块**表示HyperNet
- 内部画5层卷积的堆叠 (用渐变色表示通道数减少: 1920→512→256→112)
- **Dropout层**用**黄色虚线框**标注"Dropout 0.4 (Training only)"
- 右侧画5个小箭头指向动态权重: `w1,b1` ~ `w5,b5`
- 标注"Dynamic Weight Generation"

---

### 模块5: TargetNet (质量预测)

**结构 (原始和改进基本相同)**:

```
┌─────────────────────────────────────────────────────────┐
│ TargetNet: 使用动态权重的前向网络                       │
│                                                         │
│ Input: target_in_vec [B, 112, 1, 1]                     │
│   ↓                                                     │
│   FC1 (动态权重): 112 → 16                              │
│   ├─ weight: w1 [B, 16, 112, 1, 1]                     │
│   ├─ bias:   b1 [B, 16]                                │
│   └─ ReLU + Dropout(0.5) ◀── 改进: 新增Dropout ✅       │
│   ↓                                                     │
│   FC2 (动态权重): 16 → 8                                │
│   └─ ReLU + Dropout(0.5) ◀── 改进: 新增Dropout ✅       │
│   ↓                                                     │
│   FC3 (动态权重): 8 → 4                                 │
│   └─ ReLU + Dropout(0.5) ◀── 改进: 新增Dropout ✅       │
│   ↓                                                     │
│   FC4 (动态权重): 4 → 2                                 │
│   └─ ReLU + Dropout(0.5) ◀── 改进: 新增Dropout ✅       │
│   ↓                                                     │
│   FC5 (动态权重): 2 → 1                                 │
│   ↓                                                     │
│ Output: Quality Score [B, 1]                            │
│                                                         │
│ 改进:                                                   │
│ ✅ 每层后加Dropout(0.5)防止过拟合                       │
└─────────────────────────────────────────────────────────┘
```

**画图建议**:
- 用**绿色方块**表示TargetNet
- 画5层FC的递减结构: `112→16→8→4→2→1`
- 每层之间标注"ReLU + Dropout 0.5"
- 用**虚线**连接HyperNet生成的权重到对应的FC层
- 最后输出用**红色圆圈**表示质量分数

---

## 🎨 完整架构图布局建议

### 建议的图形布局 (从左到右):

```
┌─────────────┐    ┌──────────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────┐
│   Input     │ →  │  Swin Backbone   │ →  │ Multi-Scale  │ →  │  HyperNet    │ →  │Target│ → Score
│   Image     │    │  (4 Stages)      │    │   Fusion     │    │  (Dynamic)   │    │ Net  │
│  224×224    │    │ 128→256→512→1024 │    │ +Attention   │    │   Weights    │    │ FC×5 │
└─────────────┘    └──────────────────┘    └──────────────┘    └──────────────┘    └──────┘
                            │                       
                            │ Stage 0-3 features
                            ↓
                    ┌──────────────────┐
                    │  target_in_vec   │ (对比: 原始用LDA压缩)
                    │     (112-D)      │
                    └──────────────────┘
```

### 颜色方案建议:

| 模块 | 颜色 | 说明 |
|------|------|------|
| **Input** | 浅灰 | 输入图像 |
| **Swin Backbone** | 蓝色系渐变 | Stage 0浅蓝→Stage 3深蓝 |
| **Multi-Scale Fusion** | 橙色 | 特征融合模块 |
| **Channel Attention** | 金色 | 注意力机制(高亮) |
| **HyperNet** | 紫色 | 动态权重生成 |
| **TargetNet** | 绿色 | 质量预测网络 |
| **Output** | 红色 | 最终质量分数 |
| **Dropout** | 黄色虚线框 | 正则化 |
| **改进标记** | ✨ 星号 | 标注我们的改进点 |

---

## 📊 关键数字标注 (Base模型)

在图上标注以下关键数字:

1. **Backbone输出**:
   - Stage 0: `128-D, 56×56`
   - Stage 1: `256-D, 28×28`
   - Stage 2: `512-D, 14×14`
   - Stage 3: `1024-D, 7×7`

2. **多尺度融合**:
   - 统一后: 4×`7×7` = `196 spatial locations per scale`
   - 总通道: `128+256+512+1024 = 1920-D`

3. **注意力权重**:
   - `1024→256→4` (Softmax, sum to 1)

4. **HyperNet压缩**:
   - `1920→512→256→112` (with Dropout 0.4)

5. **TargetNet动态FC**:
   - `112→16→8→4→2→1` (with Dropout 0.5)

6. **参数量对比**:
   - 原始: `~25M parameters`
   - 改进: `~88M parameters` (Base)

---

## 🎯 关键改进点标注 (用星号或高亮)

在架构图上用**✨星号**或**高亮框**标注以下4个改进:

1. **⭐ 改进1**: `ResNet50 → Swin Transformer Base`
   - 标注: "+2.68% SRCC (87% contribution)"

2. **⭐ 改进2**: `单尺度 (2048-D) → 多尺度融合 (1920-D)`
   - 标注: "+0.15% SRCC (5% contribution)"

3. **⭐ 改进3**: `简单拼接 → Channel Attention`
   - 标注: "+0.25% SRCC (8% contribution)"

4. **⭐ 改进4**: `无正则化 → Dropout (0.4 HyperNet, 0.5 TargetNet)`
   - 标注: "防止过拟合"

**总提升**: `+3.08% SRCC (0.907 → 0.9378)`

---

## 🖼️ 建议的子图

除了主架构图，建议画3个子图:

### 子图1: Swin Transformer Block细节

```
┌────────────────────────────────────┐
│  Input: [B, C, H, W]               │
│    ↓                               │
│  Layer Norm                        │
│    ↓                               │
│  Window Multi-Head Self-Attention  │
│  (7×7 windows, local attention)    │
│    ↓                               │
│  Residual Connection               │
│    ↓                               │
│  Layer Norm                        │
│    ↓                               │
│  Shifted Window MSA                │
│  (shifted 7×7 windows, global)     │
│    ↓                               │
│  Residual Connection               │
│    ↓                               │
│  Layer Norm                        │
│    ↓                               │
│  MLP (FFN with GELU)               │
│    ↓                               │
│  Residual Connection               │
│    ↓                               │
│  Output: [B, C, H, W]              │
└────────────────────────────────────┘
```

### 子图2: Channel Attention机制

```
                      feat3 [B, 1024, 7, 7]
                             ↓
                    Global Average Pooling
                             ↓
                       [B, 1024, 1, 1]
                             ↓
        ┌────────────────────────────────────┐
        │   Attention Network                │
        │   ┌──────────────────────┐         │
        │   │ FC: 1024 → 256       │         │
        │   │ ReLU                 │         │
        │   │ Dropout(0.5)         │         │
        │   │ FC: 256 → 4          │         │
        │   │ Softmax (Σ=1)        │         │
        │   └──────────────────────┘         │
        └────────────────────────────────────┘
                             ↓
                  Attention Weights [B, 4]
                   α0   α1   α2   α3
                   ↓    ↓    ↓    ↓
              feat0·α0  feat1·α1  feat2·α2  feat3·α3
                   └────┴────┴────┘
                          ↓
              Weighted Concatenation
                          ↓
                  [B, 1920, 7, 7]
```

### 子图3: 动态权重生成机制

```
HyperNet Feature [B, 112, 7, 7]
        ↓
  ┌─────┴─────┐
  │           │
  ↓           ↓
Conv3×3      GAP + FC
  ↓           ↓
Weights     Biases
  ↓           ↓
fc_w        fc_b
[B,O,I,1,1] [B,O]
        ↓
   TargetNet Layer:
   out = conv(input, weight=fc_w, bias=fc_b)
   (动态卷积/FC)
```

---

## 📐 绘图工具建议

1. **专业工具**:
   - **draw.io** (免费，易用): https://app.diagrams.net/
   - **Figma** (协作): https://figma.com
   - **Microsoft Visio** (专业)
   - **Adobe Illustrator** (高质量)

2. **AI辅助工具**:
   - **Midjourney/DALL-E**: 用文字描述生成草图
   - **Lucidchart**: 智能流程图

3. **LaTeX (TikZ)**:
   ```latex
   \usepackage{tikz}
   \usetikzlibrary{shapes,arrows,positioning}
   ```
   优点: 矢量图，可嵌入论文，专业

4. **Python (matplotlib)**:
   - 可编程生成，适合自动化

---

## 📝 架构图Caption建议

**中文**:
> 图1: Swin-HyperIQA架构图。我们用Swin Transformer替换ResNet50作为backbone (改进1)，引入真正的多尺度特征融合 (改进2)，并使用Channel Attention动态加权 (改进3)。HyperNet和TargetNet中加入Dropout正则化 (改进4)。相比原始HyperIQA，在KonIQ-10k上提升3.08% SRCC。

**英文**:
> Figure 1: Architecture of Swin-HyperIQA. We replace ResNet50 with Swin Transformer as the backbone (Improvement 1), introduce true multi-scale feature fusion (Improvement 2), and employ Channel Attention for dynamic weighting (Improvement 3). Dropout regularization is added to HyperNet and TargetNet (Improvement 4). Compared to the original HyperIQA, our method achieves +3.08% SRCC improvement on KonIQ-10k.

---

**最后更新**: 2025-12-23  
**状态**: ✅ 完整架构说明，可直接用于绘图


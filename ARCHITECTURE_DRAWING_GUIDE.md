# SMART-IQA 架构图绘制详细指南

## 📖 首先理解原论文架构

### HyperIQA的数据流程

```
输入图像
    ↓
ResNet-50 (4个stages)
    ├─→ Stage 1 (256 channels) ──→ LDA Module ──┐
    ├─→ Stage 2 (512 channels) ──→ LDA Module ──┤
    ├─→ Stage 3 (1024 channels) ─→ LDA Module ──┤→ GAP → Flatten → Concat → Target Network Input
    │                                            │                                    ↓
    └─→ Stage 4 (2048 channels) ─────────────────┴────────────────→ HyperNet      FC1 → FC2 → FC3 → FC4
                                                                        ↓              ↑
                                                                    Conv1×1×3      动态权重+偏置
                                                                        ↓
                                                            生成Target Network的权重和偏置
```

**关键点**：
1. **前3个stage** → LDA Module → GAP → Flatten → Concat → **输入TargetNet**
2. **第4个stage** → 直接进入 **HyperNet**
3. **HyperNet** (3个1×1卷积) → 生成TargetNet的权重和偏置
4. **TargetNet** (4个FC层) → 使用动态权重 → 输出质量分数

---

## 🎨 我们的SMART-IQA改动

### 关键改进点

1. **ResNet-50 → Swin Transformer** (4个stages)
2. **LDA Module → 改进的Multi-scale Feature Extraction**
3. **新增：Channel Attention机制** ⭐
   - 用Stage 4生成attention权重
   - 对前3个stage的特征进行加权
4. **其他保持不变**

---

## 🖼️ 完整架构图绘制步骤

### 第一步：准备画布（PPT）

```
1. 新建PPT，页面比例16:9
2. 页面大小：宽30cm × 高17cm（方便后续缩放）
3. 背景：白色
4. 字体：Arial或Times New Roman
```

---

### 第二步：从左到右画整体布局

#### 布局草图
```
┌────────┐   ┌──────────┐   ┌─────────────┐   ┌──────────┐   ┌──────────┐   ┌────────┐
│ Input  │→→→│  Swin    │→→→│ Multi-scale │→→→│Attention │→→→│ HyperNet │→→→│Quality │
│ Images │   │Transform.│   │ Features    │   │  Fusion  │   │    +     │   │ Score  │
│        │   │ (4 stage)│   │ Extraction  │   │          │   │TargetNet │   │        │
└────────┘   └──────────┘   └─────────────┘   └──────────┘   └──────────┘   └────────┘
```

---

### 第三步：详细绘制每个模块

## 模块1：输入图像区域（最左侧）

### 画法：
```
1. 插入3张图片（纵向排列）：
   - data/example_3.jpg (低质量)
   - data/example_2.jpg (中质量)
   - data/example_1.jpg (高质量)
   
2. 尺寸：每张约 2cm × 1.5cm
3. 上方标注："Input Images"
4. 每张图下方标注MOS分数
```

---

## 模块2：Swin Transformer骨干网络

### 画法：用4个立方体表示

**Step 2.1: 画立方体**
```
PPT操作：
1. 插入 → 形状 → 基本形状 → 立方体
2. 复制4个，从左到右排列
3. 颜色渐变：
   - Stage 1: 浅蓝 #E3F2FD
   - Stage 2: 中蓝 #90CAF9
   - Stage 3: 深蓝 #42A5F5
   - Stage 4: 最深 #1976D2

4. 尺寸（体现分辨率）：
   - Stage 1: 3.5cm高 × 2.5cm宽
   - Stage 2: 3.0cm高 × 2.2cm宽
   - Stage 3: 2.5cm高 × 1.8cm宽
   - Stage 4: 2.0cm高 × 1.5cm宽
```

**Step 2.2: 添加标注**
```
每个立方体上方：
- Stage 1
  96×56×56
  
- Stage 2
  192×28×28
  
- Stage 3
  512×14×14
  
- Stage 4
  1024×7×7

顶部标题："Swin Transformer Backbone"
```

---

## 模块3：Multi-scale Feature Extraction

### 画法：只对前3个stage

**关键**：Stage 4不经过这个模块！

```
从Stage 1, 2, 3各引出一条箭头：

┌─────────────────────────────┐
│ Multi-scale Feature         │
│ Extraction Module           │
│                             │
│ Stage 1,2,3 → Adaptive Pool│
│            → 1×1 Conv       │
│            → BatchNorm      │
│            → ReLU           │
└─────────────────────────────┘
     ↓  ↓  ↓
   3个特征输出
```

**PPT操作：**
```
1. 画3个矩形框（对应3个stage）
2. 颜色：浅绿色 #C8E6C9
3. 每个框内写：
   "Adaptive Pool
    Conv 1×1
    BatchNorm + ReLU"
4. 框的尺寸：2cm × 1.5cm
```

---

## 模块4：Channel Attention（新增！最重要）⭐

### 这是我们的核心创新！

**数据流**：
```
Stage 4特征 (1024×7×7)
    ↓
Global Average Pooling → [1024×1×1]
    ↓
Attention Network:
    Linear(1024 → 256)
    ReLU
    Dropout(0.5)
    Linear(256 → 3)  ← 注意：生成3个权重（对应Stage 1,2,3）
    Softmax
    ↓
[w1, w2, w3] ← 3个attention权重
    ↓
应用到Stage 1,2,3的特征上
```

**PPT画法（重点！）**：

```
1. 画一个醒目的矩形框
   - 颜色：橙色边框#FF9800，浅橙色填充#FFE0B2
   - 尺寸：4cm × 3cm
   - 位置：在Multi-scale Features和HyperNet之间

2. 框内内容：
   ┌──────────────────────────────┐
   │  Channel Attention Fusion    │
   │                              │
   │  Stage 4 GAP → [1024]        │
   │       ↓                      │
   │  Linear(1024→256) → ReLU     │
   │       ↓                      │
   │  Linear(256→3) → Softmax     │
   │       ↓                      │
   │  Weights: [w1, w2, w3]       │
   │                              │
   │  Apply to Stage 1,2,3        │
   └──────────────────────────────┘

3. 画加权示意：
   从Stage 1,2,3的特征引出箭头
   → 经过Attention框
   → 标注 ×w1, ×w2, ×w3
   → 输出加权后的特征
```

---

## 模块5：特征汇聚与输入

### 关键流程

```
加权后的Stage 1,2,3特征
    ↓
Concatenate
    ↓
输入到 Target Network
```

**PPT画法**：
```
1. 画一个"⊕"符号（表示concat）
2. 标注："Weighted Concat"
3. 箭头指向Target Network
```

---

## 模块6：HyperNet（关键！）

### HyperNet的输入和输出

**输入**：Stage 4的Semantic Feature（1024×7×7）

**结构**：
```
Stage 4 Feature (1024×7×7)
    ↓
Conv 1×1 (1024 → 512) + ReLU
    ↓
Conv 1×1 (512 → 256) + ReLU
    ↓
Conv 1×1 (256 → 112) + ReLU
    ↓
生成4组权重和偏置：
  - FC1 weight & bias
  - FC2 weight & bias
  - FC3 weight & bias
  - FC4 weight & bias
```

**PPT画法**：

```
1. 画一个大矩形框
   - 颜色：绿色边框#4CAF50，浅绿填充#C8E6C9
   - 尺寸：3.5cm × 4cm
   - 标题："Content Understanding HyperNet"

2. 内部画3层：
   ┌────────────────────┐
   │ Conv 1×1 (→512)    │
   ├────────────────────┤
   │ Conv 1×1 (→256)    │
   ├────────────────────┤
   │ Conv 1×1 (→112)    │
   └────────────────────┘

3. 从HyperNet引出4条箭头到TargetNet，标注：
   → "FC1 weights + bias"
   → "FC2 weights + bias"
   → "FC3 weights + bias"
   → "FC4 weights + bias"
```

---

## 模块7：Target Network

### TargetNet的结构

**输入**：Concatenated features（来自加权的Stage 1,2,3）

**网络结构**：
```
Input Features
    ↓
FC1 (224 → 112) ← 使用HyperNet生成的权重
    ↓
FC2 (112 → 56) ← 使用HyperNet生成的权重
    ↓
FC3 (56 → 28) ← 使用HyperNet生成的权重
    ↓
FC4 (28 → 1) ← 使用HyperNet生成的权重
    ↓
Quality Score
```

**PPT画法**：

```
1. 画一个大矩形框
   - 颜色：紫色边框#9C27B0，浅紫填充#E1BEE7
   - 尺寸：3cm × 5cm
   - 标题："Quality Prediction Target Network"

2. 内部画4个FC层（梯形状）：
   ┌─────────────┐
   │    FC1      │ ← Weights from HyperNet
   ├─────────────┤
   │    FC2      │ ← Weights from HyperNet
   ├─────────────┤
   │    FC3      │ ← Weights from HyperNet
   ├─────────────┤
   │    FC4      │ ← Weights from HyperNet
   └─────────────┘

3. 每个FC层旁边画两个小框：
   ┌───────┐ ┌──────┐
   │Weights│ │ Bias │
   └───────┘ └──────┘
   标注："From HyperNet"
```

---

## 模块8：输出质量分数

### 画法

```
1. 在最右侧放置3张图片（与输入对应）
2. 每张图片下方标注：
   - Predicted: XX.XX
   - Ground Truth: X.XX
3. 上方标题："Quality Predictions"
```

---

## 🎨 完整图的组装

### 连接箭头的画法

**关键连接**：

```
1. Input → Swin Transformer (粗箭头)
2. Swin的4个stage → 分别引出箭头：
   - Stage 1,2,3 → Multi-scale Feature Extraction
   - Stage 4 → 分叉：
     * 一条到Attention Network
     * 一条到HyperNet

3. Multi-scale Features → Attention Fusion
4. Attention加权后 → Concat → Target Network
5. HyperNet → 4条箭头到Target Network（每个FC层）
6. Target Network → Quality Score
```

**箭头样式**：
```
- 主流程：粗箭头（3pt）
- 权重传递：虚线箭头（2pt）
- 特征流：实线箭头（2pt）
- 颜色：深灰 #424242
```

---

## 🎯 与原论文的视觉对比

### 用颜色区分改进点

**保持原样的部分**（灰色/蓝色）：
- HyperNet结构
- Target Network结构
- 整体pipeline

**改进的部分**（高亮颜色）：
- Swin Transformer（用蓝色渐变突出）
- **Channel Attention**（用橙色边框突出）⭐
- Multi-scale Feature（改进后用绿色）

---

## 📝 图例和标注

### 在图的底部添加图例

```
┌────────────────────────────────────────┐
│ 图例 (Legend)                          │
│                                        │
│ [蓝色方块] Swin Transformer Stage      │
│ [橙色框] Channel Attention (Ours) ⭐  │
│ [绿色框] Multi-scale Feature           │
│ [紫色框] Target Network                │
│ ─────→  Feature Flow                   │
│ ─ ─ →   Dynamic Weights                │
└────────────────────────────────────────┘
```

---

## 🔍 细节优化建议

### 1. 字体大小
```
- 主标题：14pt Bold
- 模块名：12pt Bold
- 层名称：10pt
- 标注说明：9pt
```

### 2. 对齐
```
- 所有模块的顶部对齐
- 箭头的起点和终点对齐
- 文字居中对齐
```

### 3. 间距
```
- 模块之间：1-1.5cm
- 箭头长度：0.5-1cm
- 文字与框的距离：0.2cm
```

### 4. 突出重点
```
- 用红色星标⭐标注创新点
- Attention模块用粗边框
- 重要的连接用粗箭头
```

---

## 📐 精确尺寸参考

### 整体画布
```
总宽度：28cm
总高度：15cm
```

### 各模块宽度分配
```
Input Images:        2cm
Swin Transformer:    6cm
Multi-scale:         4cm
Attention:           4cm
HyperNet+TargetNet:  8cm
Output:              2cm
间距：               2cm (总共)
```

---

## ✅ 检查清单

画完后检查：

- [ ] Swin Transformer有4个stage且标注了维度
- [ ] 只有Stage 1,2,3进入Multi-scale Feature Extraction
- [ ] **Attention模块醒目且流程清晰** ⭐
- [ ] Stage 4分为两路：一路Attention，一路HyperNet
- [ ] HyperNet有3个Conv 1×1层
- [ ] HyperNet输出4组权重到TargetNet
- [ ] TargetNet有4个FC层
- [ ] 所有箭头方向正确
- [ ] 颜色区分清晰
- [ ] 文字无错别字

---

## 🎨 PPT快捷操作技巧

### 对齐技巧
```
1. 选中多个对象
2. 右键 → 对齐 → 顶端对齐/居中对齐
3. Ctrl+D 快速复制
4. Alt+拖动 创建副本
```

### 颜色复制
```
1. 选中对象
2. 格式刷 (Ctrl+Shift+C 复制格式)
3. Ctrl+Shift+V 粘贴格式
```

### 组合对象
```
1. 选中多个相关对象
2. Ctrl+G 组合
3. 方便整体移动
```

---

## 💡 最后提示

**最重要的3个创新点一定要突出**：

1. **Swin Transformer替代ResNet** - 用蓝色渐变
2. **Channel Attention机制** - 用橙色醒目边框 ⭐⭐⭐
3. **Adaptive Multi-scale Fusion** - 用绿色标注

这张图要让reviewer一眼看出我们的改进在哪里！

---

## 参考数据流

```python
# 伪代码表示完整流程
image → Swin Transformer → [stage1, stage2, stage3, stage4]

# 前三个stage
stage1, stage2, stage3 → Multi-scale_Feature_Extraction → [f1, f2, f3]

# Stage 4 用于生成attention权重
stage4 → GAP → Attention_Network → [w1, w2, w3]

# 加权融合
weighted_f1 = f1 * w1
weighted_f2 = f2 * w2
weighted_f3 = f3 * w3
target_input = Concat([weighted_f1, weighted_f2, weighted_f3])

# HyperNet生成权重
stage4 → HyperNet → {fc1_w, fc1_b, fc2_w, fc2_b, fc3_w, fc3_b, fc4_w, fc4_b}

# Target Network预测
quality_score = TargetNet(target_input, weights_from_hypernet)
```

**现在开始画图吧！有任何具体的模块不清楚随时问我！** 🎨

